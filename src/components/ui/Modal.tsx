"use client";

import React from "react";
import {
  Dialog as MTDialog,
  DialogHeader as MTDialogHeader,
  DialogBody as MTDialogBody,
  DialogFooter as MTDialogFooter,
} from "@material-tailwind/react";

export const Dialog = MTDialog as React.FC<any>;
export const DialogHeader = MTDialogHeader as React.FC<any>;
export const DialogBody = MTDialogBody as React.FC<any>;
export const DialogFooter = MTDialogFooter as React.FC<any>;
